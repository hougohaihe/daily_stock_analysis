# Core dependencies for daily stock analysis
akshare>=1.10.0
pandas>=1.5.0
numpy>=1.23.0
requests>=2.28.0

# Data visualization
matplotlib>=3.6.0
seaborn>=0.12.0
plotly>=5.11.0

# Scheduling and automation
APScheduler>=3.9.0
schedule>=1.1.0

# Environment and configuration
python-dotenv>=0.21.0

# Email notifications
# Note: secure-smtplib is unmaintained; using smtplib from stdlib instead
# secure-smtplib>=0.1.1

# Database support
sqlalchemy>=1.4.0

# Logging
loguru>=0.6.0

# Testing
pytest>=7.2.0
pytest-cov>=4.0.0

# Linting and formatting
flake8>=6.0.0
black>=22.10.0

# Personal additions
jupyter>=1.0.0  # for interactive exploration and experimentation
ipywidgets>=8.0.0  # enables interactive widgets in Jupyter notebooks
nbconvert>=7.0.0  # useful for exporting notebooks to HTML/PDF for sharing

# Extra tools I find useful personally
rich>=12.0.0  # pretty terminal output, helpful when running scripts from the command line
openpyxl>=3.0.0  # needed for pandas to read/write .xlsx files (ran into this issue exporting results)
